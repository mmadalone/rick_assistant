#!/usr/bin/env zsh
# _rick_menu_symbol - Get a menu symbol with Unicode/ASCII fallback
#
# Usage:
#   _rick_menu_symbol <symbol_name>
#
# Arguments:
#   symbol_name - The name of the symbol to retrieve
#
# Returns:
#   Prints the symbol to stdout
#
# Example:
#   _rick_menu_symbol "arrow"    # Returns → or -> depending on terminal support

# Function implementation
_rick_menu_symbol() {
  local symbol="$1"
  
  # If no symbol specified, return empty
  [[ -z "$symbol" ]] && return 1
  
  # Use Unicode if supported, otherwise ASCII fallback
  if (( _RICK_MENU_SUPPORTS_UNICODE )); then
    echo -n "${RICK_MENU_SYMBOLS[$symbol]}"
  else
    echo -n "${RICK_MENU_ASCII[$symbol]}"
  fi
}

# Call the function with the arguments passed to this script
_rick_menu_symbol "$@" 